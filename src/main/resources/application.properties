spring.application.name=bugtrackpro

# ── Active profile (override with SPRING_PROFILES_ACTIVE=prod in Railway) ──
spring.profiles.active=${SPRING_PROFILES_ACTIVE:default}

# ── Database (env vars set in Railway; local fallback values below) ──────────
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/bugtrackpro}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Abhi@1631}

# ── JPA ──────────────────────────────────────────────────────────────────────
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.show-sql=${SHOW_SQL:true}

# ── Server ────────────────────────────────────────────────────────────────────
server.port=${PORT:8080}

# ── JWT ───────────────────────────────────────────────────────────────────────
jwt.secret=${JWT_SECRET:5468756B58703273357638792F423F4528482B4D6251655468576D5A71347437}

# ── CORS allowed origin (set to your Netlify URL in production) ───────────────
cors.allowed-origin=${CORS_ORIGIN:*}
