/**
 * config.js — Environment configuration
 *
 * LOCAL DEV:  BASE_URL = 'http://localhost:8080'  (default)
 * PRODUCTION: Replace with your Railway backend URL before deploying to Netlify
 *             e.g.  'https://bugtrackpro-production.up.railway.app'
 */
const CONFIG = {
  BASE_URL: 'http://localhost:8080',
};
